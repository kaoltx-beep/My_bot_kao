name: Developer Mode Checks

on:
  push:
    branches: [main, developer-mode-complete]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Compile core modules
        run: |
          python -m py_compile run.py developer_mode.py developer_mode_router.py dev_handler.py code_analyzer.py
      - name: Run tests
        run: python -m unittest discover -s tests -p 'test_*.py'
